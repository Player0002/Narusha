스텝모터
	스텝 4 모터 스텝핑 모터
여기서 이거 S극 N극이니까 영구자석 좌우는 전자석 그래서 전류를 흘려준ㄴ 양에 딸라서 왼손법칙에 따라 힘이 어느 방향으로 가는 지에 따라 결과가 정해진다.
DC 모터할때 자세히 배우자.

전류를 흘리므로 영구자석 S극에 전자석 N극이 붙게 된다.
영구자석 N에는 S가 붙게 된다.
대각선 방향으로 발싸하면 N극은 중간으로

전류를 어느방향으로 주는지에 따라서 방향이 정해진다 ( Step )
전류를 넣어주는 방법? 실제 극성이 연결 되어 있을때, 전류가 붙어있고 붙어있고 2개가 떨어져있다.
펄스가 돌다가 나오고 내부적으로 모두 다 연결되어 있다.

스위치가 TR에 연결되어 있다.
그러면 1초마다 변경해주면 1초마다 변경
10ms 마다 해주면 부드럽게
빠르면 빠를수록 빠르게 돌아간다.

빨리 돌릴려면? 빠른 속도로 넣어주면 된다. 

이론상 4스텝으로 했고, 실제 스텝 모터는 360도 도는데 200개 스텝 
200개 들어갔을때 360도 중요함.

이러한 역할을 해주는 것이 드라이버다 ( 스텝모터 드라이버 )

반대방향 -> 순서 변경

4분의 1스텝을 넣으려면?

2분의 1스텝과 1스텝 사이에 놓을려면?

전자석의 세기를 달리하면 된다.
이러한 방식으로 스텝을 조절한다.

한스텝에 1ms 라면 반은 0.5 1/4는 0.25

한바퀴에 200스텝 -> 
	  벨트에 연결된 톱니바퀴가 16개 (홈이 파인 개수)
	=>한 톱니와 톱니 사이가 2mm 라고 하면, 한바퀴 도는데 32mm 3.2cm 
	  32 / 200 -> 0.16mm   

	200 / ( 1 / 16 ) -> 3200 

	한스텝
	32 / 3200 -> 0.01 mm 

	5cm 갈려면? 5000 스텝  ( 50 mm ) 

	200us 이상으로 넣어야함.
	우선은 1ms 로 테스트


direction HIGH 
for(int i = 0; i < 5000; i ++)
	digitalWrite(12, HIGH);
	delayMicroseconds(1000); // 나중에 줄일 예정
	digitalWrite(12, LOW);
	delayMicroseconds(1000);

ramps 1.4 pin map

digitalWrite(55, LOW); // X_DIR
digitalWrite(38, LOW); // X_ENABLE
while(1)
	digitalWRite(54, HIGH);
	delayMicroseconds(200);
	digitalWRite(54, LOW);
	delayMicroseconds(200);

X_MIN_PIN 에 HIGH 를 주면 내부적으로 PULL UP 회로가 구성됩니다.
이 방식은 매우 불안정 하므로 왠만해선 외부에 회로를 구성하는것이 좋으며, 만약 구성할 수 없는 상황일때 유용하게 사용할 수 있습니다. 한때 삼성 에어컨이 내부 풀업으로 인해 리콜사태가 일어난적이 있습니다.